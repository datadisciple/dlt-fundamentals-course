[project]
name = "dlt-fundamentals-course"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dlt[duckdb]>=1.5.0",
    "pyarrow>=19.0.0",
]
